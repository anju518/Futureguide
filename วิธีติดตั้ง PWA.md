# ติดตั้งเป็นแอป (PWA) — ระบบแนะแนวการศึกษาและอาชีพ

แอปนี้เป็น PWA ที่ติดตั้งบน iOS / Android / iPad / แท็บเล็ต ได้เหมือนแอปจริง
เปิดออฟไลน์ได้ และถ้าไม่มีเน็ตตอนบันทึก จะ **เก็บคิวไว้แล้วส่งขึ้น Google Sheets อัตโนมัติเมื่อมีเน็ต**

## ไฟล์ในชุดนี้ (ต้องอยู่โฟลเดอร์เดียวกัน)

- `index.html` — ตัวแอป (เดิมชื่อ FutureGuide.html)
- `manifest.json` — ข้อมูลแอป + ไอคอน
- `service-worker.js` — ทำให้เปิดออฟไลน์ได้
- `icon-192.png`, `icon-512.png` — ไอคอนแอป (โลโก้โรงเรียน)

> ต้องอัปทั้ง 5 ไฟล์ขึ้นเว็บที่เป็น **HTTPS** (PWA บังคับ)

---

## วิธีที่ 1: Netlify (ง่ายสุด ไม่ต้องมีบัญชี Git)

1. เข้า https://app.netlify.com/drop
2. ลาก **ทั้ง 5 ไฟล์** (หรือทั้งโฟลเดอร์) ไปวาง
3. ได้ลิงก์ HTTPS ทันที เช่น `https://ชื่อสุ่ม.netlify.app`
4. เปิดลิงก์บนมือถือ → ติดตั้งเป็นแอป (ดูด้านล่าง)

## วิธีที่ 2: GitHub Pages (ฟรีถาวร)

1. สร้าง repository ใหม่บน GitHub → อัปโหลด 5 ไฟล์
2. Settings → Pages → Source: `main` / root → Save
3. ได้ลิงก์ `https://ชื่อผู้ใช้.github.io/ชื่อ-repo/`
   (path แบบ relative จึงใช้ได้เลยแม้อยู่ในซับโฟลเดอร์)

## วิธีที่ 3: Firebase Hosting / Cloudflare Pages
เหมาะถ้าโรงเรียนใช้ Google Workspace หรืออยากได้โดเมนของตัวเอง — อัปทั้งโฟลเดอร์เช่นกัน

> ⚠️ อย่าใช้ Google Sites — เปิด PWA/Service Worker ไม่ได้

---

## ติดตั้งลงเครื่อง (หลังเปิดลิงก์)

**iPhone / iPad (Safari):**
แชร์ (□↑) → "เพิ่มไปยังหน้าจอโฮม" → เพิ่ม

**Android (Chrome):**
เมนู ⋮ → "ติดตั้งแอป" / "เพิ่มลงในหน้าจอหลัก"

จะได้ไอคอนโลโก้โรงเรียนบนหน้าจอ เปิดเต็มจอเหมือนแอปจริง

---

## การบันทึกข้อมูล (ออนไลน์/ออฟไลน์)

- **มีเน็ต** → บันทึกขึ้น Google Sheets ทันที
- **ไม่มีเน็ต** → เก็บไว้ในเครื่อง ขึ้นว่า "ออฟไลน์ – จะส่งให้อัตโนมัติเมื่อมีเน็ต"
  ปุ่มบันทึกในโปรไฟล์จะโชว์จำนวนที่ค้าง เช่น "(ค้าง 2 รายการ)"
- **เน็ตกลับมา** (ระหว่างเปิดแอป หรือเปิดแอปครั้งถัดไป) → ส่งคิวที่ค้างให้อัตโนมัติ

> หมายเหตุ: อย่าลืมตั้งค่าฝั่ง Google Apps Script ให้เขียนลงชีต/Drive ตามไฟล์ "วิธีตั้งค่า Google Sheets.md"
> (URL ของ Web App ฝังไว้ในแอปแล้ว)

---

## อัปเดตแอปในอนาคต
ถ้าแก้ไฟล์แล้วอัปใหม่ ให้แก้เลข version ใน `service-worker.js` (`futureguide-v1` → `-v2`)
เพื่อให้เครื่องผู้ใช้โหลดเวอร์ชันใหม่
